while true do
    -- Функция для первого скрипта
local function one()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local bald = dropdownFrame:WaitForChild("Bald")
local iconButton = bald:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Сайтама"
end

-- Функция для второго скрипта
local function two()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local batter = dropdownFrame:WaitForChild("Batter")
local iconButton = batter:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Плохой"
end

-- Функция для третьего скрипта
local function three()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local blade = dropdownFrame:WaitForChild("Blade")
local iconButton = blade:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Камикадзе"
end

-- Функция для четвертого скрипта
local function four()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local cyborg = dropdownFrame:WaitForChild("Cyborg")
local iconButton = cyborg:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Генос"
end

-- Функция для пятого скрипта
local function five()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local esper = dropdownFrame:WaitForChild("Esper")
local iconButton = esper:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Тацумаки"
end

-- Функция для пятого скрипта
local function six()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local hunter = dropdownFrame:WaitForChild("Hunter")
local iconButton = hunter:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Гароу"
end

-- Функция для пятого скрипта
local function seven()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local kj = dropdownFrame:WaitForChild("KJ")
local iconButton = kj:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Кдж"
end

-- Функция для пятого скрипта
local function eight()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local ninja = dropdownFrame:WaitForChild("Ninja")
local iconButton = ninja:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Соник"
end

-- Функция для пятого скрипта
local function nine()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local purple = dropdownFrame:WaitForChild("Purple")
local iconButton = purple:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Суйрю"
end

-- Функция для пятого скрипта
local function ten()
    -- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local tech = dropdownFrame:WaitForChild("Tech")
local iconButton = tech:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Ребенок-император"
end

-- Функция для пятого скрипта
local function eleven()
-- Получаем локального игрока
local player = game.Players.LocalPlayer

-- Пытаемся получить доступ к нужным объектам с проверками
local topbarPlus = player:WaitForChild("PlayerGui"):WaitForChild("TopbarPlus")
local topbarContainer = topbarPlus:WaitForChild("TopbarContainer")
local unnamedIcon = topbarContainer:WaitForChild("UnnamedIcon")
local dropdownContainer = unnamedIcon:WaitForChild("DropdownContainer")
local dropdownFrame = dropdownContainer:WaitForChild("DropdownFrame")
local sorcerer = dropdownFrame:WaitForChild("Sorcerer")
local iconButton = sorcerer:WaitForChild("IconButton")

-- Получаем IconLabel и меняем текст
local iconLabel = iconButton:WaitForChild("IconLabel")
iconLabel.Text = "Годжо сатору"
end
-- Запускаем все скрипты последовательно
one()
two()
three()
four()
five()
six()
seven()
eight()
nine()
ten()
eleven()
    wait(0.1) -- Ждем 5 секунд
end
